import { createStyles } from "@mantine/core";

export const useMoreLikeThisStyles = createStyles(() => ({
  root: {
    marginTop: 80,
    marginBottom: 12,
    width: "100%",
  },
  productsContainer: {
    overflowX: "auto",
    width: "100%",
    marginTop: 24,
    gap: 24,
  },
}));
